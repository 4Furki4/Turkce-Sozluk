# Compatibility compose file for the Raspberry Pi development deployment.
# Prefer docker-compose.development.yml and docker-compose.production.yml for
# running both branches side by side.
include:
  - docker-compose.development.yml
